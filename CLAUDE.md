# Priced Claude Agent Entry Point

Before making any change, read `AGENTS.md` and then `INTEGRATION_NOW.md`.

`AGENTS.md` contains the current product rules, Supabase project state, Dodo approval and integration state, Vercel state, free-tier environment strategy, expected environment variables, security constraints, execution sequencing, test commands, and completion criteria.

Do not rely on older checklist wording when it conflicts with `AGENTS.md` or `INTEGRATION_NOW.md`.

Do not recreate completed infrastructure, do not expose secrets, do not enable Priced Credits, do not change the pricing formula, and do not create paid resources without explicit owner authorization.
